[project]
name = "lanchain-learning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Akash Gautam", email = "akashgautam01584@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "jinja2>=3.1.6",
    "langchain>=1.4.0",
    "langchain-openai>=1.6.2",
    "python-dotenv>=1.0.0",
]

[project.scripts]
lanchain-learning = "lanchain_learning:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
